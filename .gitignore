# Migration pipeline generated output (local SQL dump extracts)

# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
dist/
build/
out/
.turbo/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
# macOS APFS is case-insensitive: `ENV/` matches `src/lib/env/` — un-ignore app source there.
!src/lib/env/
!src/lib/env/**
.uv/
*.egg-info/
.eggs/

# Environment
.env
.env.local
.env.*.local
.env.*
!.env.example

# Claude Memory (personal settings)
CLAUDE.local.md

# IDE
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Testing
coverage/
.nyc_output/
.pytest_cache/
htmlcov/
.coverage
.coverage.*
test-results/
playwright-report/
playwright/.cache/
.playwright-mcp/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Supabase
supabase/.branches
supabase/.temp

# Docker
.docker/

# Misc
*.tsbuildinfo
.vercel
.cache
nul

# Agent / IDE local artifacts (not part of production repo)
.readiness-architect/
.session-handoff/
.harness/
.mcp.json
.autogit.json
.lighthouseagenticrc.json

# Session screenshots and ad-hoc notes (UI review artifacts)
*.png
*.jpg
*.jpeg
# Ship raster assets under public/ (Docker/runtime); global *.png/*.jpg would otherwise omit them from git.
!public/**/*.png
!public/**/*.jpg
!public/**/*.jpeg
login-section.md

# Backend-level Claude hook shim (auto-generated to fix CWD drift)
apps/backend/.claude/

# Skills (external cloned repos)
.skills/vercel-labs-agent-skills/

# Local binaries (Beads CLI)
.bin/

# AI Review (generated outputs)
ai-review/results/
ai-review/screenshots/

# Demo screencast pipeline (generated recordings, composites, results manifest)
output/demo-screencast/

# Local video pipeline scratch audio (regenerated by video:* scripts)
.video-work/

# Generated print artifacts (regenerated on demand; not source)
output/pdf/

# Course voiceover pipeline — resumable run state (briefs themselves are committed)
data/voice/course-voice-results.json

# Licensed third-party media downloaded for video production (e.g. Artlist) — never commit
assets/artlist/

# NotebookLM generated artifacts
.claude/notebooklm/repo-pack.txt
.claude/data/minion-state.json
.worktrees/
.env*.local
# WordPress migration export (legacy one-time migration; live scrape retired — see docs/agent-framework/CONNECTORS.md). courses.json is tracked for deploy seeds.
data/wordpress-export/*
!data/wordpress-export/courses.json
!data/wordpress-export/carsi.WordPress.2026-04-05.xml.gz

# Prisma 7 generated client (`npm run postinstall` / `prisma generate`)
src/generated/prisma/
