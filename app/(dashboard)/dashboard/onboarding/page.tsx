import { OnboardingHubClient } from '@/components/onboarding/OnboardingHubClient';

export const metadata = {
  title: 'Maintenance Company Onboarding | CARSI',
  description:
    'Enterprise workforce onboarding for facility management organisations — operational readiness, safety, and professional standards.',
};

export default function OnboardingHubPage() {
  return <OnboardingHubClient />;
}
