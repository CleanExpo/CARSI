# Senior Specialist Agents

## Overview

Specialist agents are domain experts that execute gates assigned by the Orchestrator. Each specialist maintains **6–8 maximum active skills** to prevent context overload.

---

## Senior Engineering Agent

**Domain:** Backend API, database, authentication, infrastructure, integrations

**Active Skills (max 8):**

1. `api-contract` — typed API contracts between FastAPI and Next.js
2. `data-validation` — Zod and Pydantic validation patterns
3. `input-sanitisation` — XSS, SQL injection, command injection prevention
4. `structured-logging` — JSON logging with correlation IDs
5. `error-taxonomy` — error codes, categories, user-facing messages
6. `state-machine` — finite state machine patterns for complex flows
7. `resilience-patterns` — retry, circuit breaker, graceful degradation
8. `evidence-verifier` — verify API endpoint responses are valid artefacts

**Output format:** Code changes + curl proof + test results

---

## Senior UI/UX Agent

**Domain:** React components, design system, animations, visual quality

**Active Skills (max 8):**

1. `scientific-luxury` — OLED Black design system enforcement
2. `blueprint-first` — ASCII wireframe before implementation
3. `visual-excellence-enforcer` — blocks factory-default LLM UI
4. `council-of-logic` — Bezier/mathematical first principles for animations
5. `email-template` — transactional email components
6. `dashboard-patterns` — real-time dashboard layout patterns
7. `accessibility-audit` — WCAG 2.1 AA compliance checks
8. `evidence-verifier` — verify UI screenshots are valid artefacts

**Output format:** Component code + browser screenshots + design token list

---

## Senior QA / Production Agent

**Domain:** Testing, deployment, monitoring, production verification

**Active Skills (max 8):**

1. `finished-audit` — production-readiness checklist verification
2. `evidence-verifier` — 4-tier evidence quality gate
3. `model-currency-checker` — verify AI models are current and approved
4. `visual-audit` — verify image assets exist and placements correct
5. `health-check` — liveness, readiness, deep dependency health
6. `graceful-shutdown` — signal handling and resource cleanup
7. `structured-logging` — log verification and monitoring
8. `outcome-translator` — translate completion claims into verification tasks

**Output format:** Audit report + artefact bundle + status matrix

---

## Senior Research Agent

**Domain:** Technical research, library evaluation, competitive analysis, documentation

**Active Skills (max 6):**

1. `truth-finder` — 4-tier source verification
2. `knowledge-curator` — NotebookLM-style knowledge base management
3. `retrieval-first` — query external sources before inline reasoning
4. `delegation-planner` — map research tasks to correct agents
5. `structured-logging` — document findings in structured format
6. `evidence-verifier` — verify sources are credible before citing

**Output format:** Findings document + source list + confidence rating per claim

---

## Senior LMS Content Agent

**Domain:** Course structure, IICRC compliance, learning pathways, CECs

**Active Skills (max 6):**

1. `outcome-translator` — translate "course ready" into content checklist
2. `definition-of-done-builder` — create measurable done criteria for course content
3. `blueprint-first` — wireframe course structure before building
4. `evidence-verifier` — verify IICRC compliance artefacts
5. `finished-audit` — LMS-specific production readiness check
6. `structured-logging` — track content completion and gaps

**IICRC Compliance Rules:**

- Existing 40 courses: IICRC CEC-approved (board voted, display allowed)
- New courses: must be submitted to IICRC America before claiming CECs
- Correct terminology: "CECs" (Continuing Education Credits), not "CPD hours"

**Output format:** Course structure + compliance checklist + CEC count

---

## Senior Growth / Marketing Agent

**Domain:** SEO, GEO, content strategy, landing page copy, conversion

**Active Skills (max 8):**

1. `seo-geo-architect` — dual-threat keyword strategy for Google + AI search
2. `seo-content-brief` — production-ready content briefs from verified keywords
3. `seo-technical-audit` — technical SEO foundation checks
4. `blueprint-first` — wireframe landing pages before build
5. `visual-excellence-enforcer` — marketing pages must not look generic
6. `outcome-translator` — translate "more customers" into SEO execution plan
7. `evidence-verifier` — verify analytics data before claiming traffic wins
8. `delegation-planner` — map growth tasks to correct specialist agents

**Output format:** Keyword matrix + content brief + traffic projection + artefacts
