# Session Handoff — 2026-07-09 13:13:38 (AEST)

**Status: READY** — this session's code work is merged + live; remaining items are planning
artifacts and founder-gated inputs. No uncommitted repo code pending.

## 1. Summary
- **Completed + shipped:** CCW-CARSI Truckmount Operations course rebuilt into an interactive LMS
  course and **published live** on carsi.com.au.
- **Completed (planning):** backlog-triage `/spm` spec; IICRC CEC-provider compliance `/spm` spec +
  IICRC request email (both in scratchpad, not yet in repo).
- **Partial / not started:** IICRC compliance Bucket A implementation (not started); backlog Bucket
  A/B execution (not started).
- **Not touched:** the ~30 founder-gated content epics; the 2 Toby emails (still awaiting send).

## 2. Where it started
- Requests (in order): "Go — wire truckmount into CARSI" → Toby feedback (remove price list; make it
  an interactive machine-selection course with flashcards/quiz/video/audio/sign-off) → "finalise, needs
  live today" → backlog `/spm` (issues + security) → IICRC CEC-provider compliance `/spm` + logos.
- Branch for the truckmount work: `feat/ccw-carsi-truckmount-lms` (merged + deleted).
- Primary checkout currently on `feat/iicrc-discipline-map-links` (unrelated pre-existing branch).
- Constraints: CARSI = IICRC **CEC provider**, never imply it delivers IICRC certification;
  AU-production standard; `main = prod` via DigitalOcean deploy-on-push; protected `main` (PR-only).

## 3. Decisions locked + what shipped
**Shipped (merged to `main`, live):**
- Truckmount course: machine-selection opener (+ inline SVG schematic), 7 unit reading lessons each
  with a flashcard deck (36 cards), 14-question auto-marked knowledge check, two-gate practical
  sign-off (LmsPracticalAssessment + 9-criterion rubric, CO zero-tolerance, records machine
  make/model/serial). CCW price list removed (lesson + HTML pack + regenerated PDF). `cecHours: 0`
  (blocks false "Earn N IICRC CECs"). Published (`status: published`). Verified live (catalogue count 2).
- Original merge was `5884cf70`; `main` has since advanced (`6be25c1b` #514 persists the CEC 0 opt-out).
**Decisions locked:**
- Phase 1 (interactive structure) live now; produced media (NotebookLM audio/video, slideshows,
  schematics) is Phase 2 via `generate-course-media.yml`.
- Machine selection = guided pick + tailored specs; machine make/model/serial captured at sign-off.
- IICRC logo insertion is GATED on CARSI's registration status + the authorised asset (see §7).

## 4. Key files
| File | Status | Note |
|---|---|---|
| `data/seed/courses-catalog.json` | Modified (merged) | truckmount course, published, cecHours 0 |
| `data/seed/ccw-truckmount.quizzes.json` | Created (merged) | 14-Q knowledge check |
| `data/seed/ccw-truckmount-assessment.json` | Created (merged) | practical sign-off + rubric |
| `scripts/seed-ccw-truckmount-assessment.ts` | Created (merged) | assessment seeder |
| `scripts/seed-all-quizzes.ts`, `deploy/Dockerfile.migrate`, `Dockerfile.migrate`, `scripts/do-migrate.sh`, `package.json` | Modified (merged) | wired quiz + assessment into PRE_DEPLOY |
| `public/courses/ccw-carsi-truckmount/course-pack.html` + PDF | Modified (merged) | price-free |
| `~/…/scratchpad/carsi-backlog-triage-spec.md` | Created (scratchpad) | backlog triage plan |
| `~/…/scratchpad/carsi-iicrc-compliance-spec.md` | Created (scratchpad) | IICRC compliance spec |
| `~/…/scratchpad/carsi-iicrc-request-email.md` | Created (scratchpad) | IICRC email draft (send-ready) |

## 5. Running state
- Branch: primary on `feat/iicrc-discipline-map-links`; scratchpad worktree `carsi-lms-wt` on detached
  `origin/main`. Tree clean (no uncommitted session code). No dev server running.
- Prod: truckmount course LIVE on carsi.com.au. No open PR from this session (all merged).
- Safe to stop: YES.

## 6. Verification
- Truckmount CI (on merged PR #499): Unit Tests / Frontend Tests / Vercel all pass (this session).
- Live: `curl -s https://carsi.com.au/courses | grep -c ccw-carsi-truckmount-operations` → 2 (this session).
- Detail: `curl -s https://carsi.com.au/courses/ccw-carsi-truckmount-operations` → real title (not 404).
- `handoff-loop.sh` not present on current branch → gate = merged-PR CI + live prod check above.

## 7. Deferred + open questions
**Deferred (owner / blocking / why):**
- IICRC Bucket A (standards-currency S520→2024/S540→2023/S700→2025, canonical disclaimer, hidden logo
  component) — owner: next session / not blocking / safe to implement now via PR.
- Backlog Bucket A (postcss `overrides` bump; P0 CEC fix #501/#504) + Bucket B verify-then-close.
- Truckmount Phase 2 produced media (audio/video/slides/schematics).
**Open questions (need Phill):**
- CARSI's exact IICRC registration (Certified Firm? Approved CE Provider mark?) + written logo
  permission + the authorised logo asset file (none in repo) — BLOCKS the "insert IICRC logos" goal.
- CARSI CEC provider ID/number + approval period (for on-site provider-status block).
- Send the IICRC email (`ceccourse@iicrcnet.org`) + the 2 Toby emails (course-link + dehumidifier reply).
- Secret-scanning alert #3 dismissal (GitHub Security tab — guardrail-blocked for the agent).

## 8. Pick up here
- **Start here:** implement IICRC Bucket A (standards-currency + canonical disclaimer + hidden
  `<IicrcMark>` component) as a PR off `origin/main`; then backlog Bucket A (postcss override + P0 CEC
  fix — but note #514 may already cover the CEC persistence, verify first).
- **Do not redo:** the truckmount course (live); do not insert IICRC logos until §7 registration +
  asset confirmed; do not `supabase db push`/direct-push to `main` (protected, PR-only).
- **First command to run:** `cd /private/tmp/…/scratchpad/carsi-lms-wt && git fetch origin main && git checkout -b feat/iicrc-compliance origin/main` then read `carsi-iicrc-compliance-spec.md`.

## 9. Risk notes
- The scratchpad worktree's `node_modules` is a symlink to the primary checkout's — regenerate Prisma
  client (`npx prisma generate`) before type-check in the worktree.
- IICRC logo usage is trademark-sensitive: only registered Certified Firms may use the IICRC logo
  (iicrc.org/invalid-firms, verified) — do NOT ship a logo without confirmed rights + the real asset.
- IICRC binding rules (provider agreement, disclaimer wording, logo spec) are NOT public — the email
  to IICRC is the source of truth; the website audit alone cannot certify "100% compliant".
- Secret-scanning alert #3 (Stripe webhook): PIPELINE.md says rotated, but RA-2989 memory flags live
  estate secrets — do not assert dead; leave for Phill to verify+dismiss.

## 10. Handoff quality check
- No faked verification: live checks + CI results are this-session tool results.
- No hidden "still running": no dev server claimed.
- Completed (truckmount live) vs deferred (IICRC/backlog implementation) is explicit.

Handoff complete. Next safe action: create `feat/iicrc-compliance` off `origin/main` and implement IICRC Bucket A (standards-currency + canonical disclaimer + hidden logo component) via PR.
