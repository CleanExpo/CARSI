# Session Handoff — CARSI notifications system + DO migration path + landing pricing + Linear reconciliation

**Timestamp:** 2026-07-02T22-23-29 · **Branch:** `main` (HEAD `a6bafa43`) · **Repo:** CleanExpo/CARSI

**Phase 0 gate:** No `scripts/handoff-loop.sh` in repo → DoD gates run manually.
- `npm run type-check` (tsc --noEmit) → **GREEN**
- `npm run test:unit` (vitest) → **GREEN, 201/201 across 36 files** (re-run after cache clear)
- `main` = all this session's PRs merged + deployed; prod `carsi.com.au/` → **HTTP 200**
- `npm run lint` **not** used as a gate — it errors locally (flat-config plugin-resolution env anomaly) while CI's identical `eslint .` is green; do NOT chase it (see §9 + prior handoff).
- Full `next build` not run locally (CI validates each PR; all green). **Verdict: READY.**

## 1. Summary of what was done
- **Built + shipped the entire notifications system (A/B/C)** — all merged, deployed, and **verified live on prod**:
  - **Phase A (#334):** `LmsNotification` model + migration, `src/lib/server/notifications.ts` (idempotent `createNotification` on unique `dedupe_key`, list/unread/markRead/markAllRead), real routes replacing the empty stubs; header bell now real data.
  - **Phase B (#335):** daily IICRC recert reminders (`/api/cron/recert-reminders` + `recert-reminders.ts`, T-30/T-7/overdue off `LmsUser.iicrcExpiryDate`, in-app + branded email).
  - **Phase C (#337):** monthly toolbox-talk drip (`/api/cron/toolbox-talk-drip` + `toolbox-drip.ts`), `LmsUser.emailOptOut` (Spam Act) + migration. (Originally #336 → merged into the wrong branch; re-landed via #337.)
- **Proved the prod migration path** end-to-end: the DO PRE_DEPLOY `prisma migrate deploy` job applies schema changes to `carsi-db` on deploy — verified via migrate-job logs (both `20260702210000_lms_notifications` and `20260702220000_user_email_opt_out` applied cleanly).
- **Shipped landing-page pricing (GP-347, #339)** — `HomePricingSection` (Free Library / Per course / Yearly $795) reusing `INDIVIDUAL_TIERS`, in the landing's theme; fixed an a11y contrast regression (slate-500→slate-600) before merge.
- **Reconciled the CARSI Linear board** (it was badly stale — product months ahead of tickets): see §3.
- Cleared ~cache/bloat and re-ran the suite green.

## 2. Where it started
Continuation of the CARSI courses/platform initiative. This segment: verify the DO migration dry-run (blocked earlier on DO access) → it turned out already-applied; then "build notifications Phase A" → B → C on the founder's "continue"; merge fixes for a stacked-PR mishap; then "update the Linear Project, pull the next tasks" → GP-347 pricing + backlog reconciliation; then a cache-clear + test re-run + this handoff. Constraints: `main = prod` via DO deploy_on_push; prod data/publish changes founder-gated; agent cannot self-merge prod PRs (founder merges).

## 3. Decisions locked + what shipped
**Shipped to `main` (all merged + deployed this session):** #334 Notifications A · #335 Notifications B · #337 Notifications C · #339 Landing pricing. HEAD `a6bafa43`.

**Linear (project 20538e04 "CARSI", team G-Pilot/GP) reconciled:**
- GP-438 **created Done** (notifications A/B/C, PRs attached).
- GP-347 (landing pricing) → **Done on #339 merge**.
- GP-250 (ToS/Privacy) → **Done** (verified `/terms`+`/privacy` = 200 live).
- GP-377 (learning pipeline) → **Done** (`.harness/learning/` exists).
- GP-421 (catalog cleanup) → **Canceled** (superseded: 72 pub/26 draft now, wrong-DB premise).
- Project **status update** posted (health onTrack).

## 4. Key files
| File | Status |
|---|---|
| `src/lib/server/notifications.ts` (+ `.test.ts`), routes `app/api/lms/notifications/**` | Created (Phase A, #334) |
| `src/lib/server/recert-reminders.ts` (+ `.test.ts`), `app/api/cron/recert-reminders/route.ts` | Created (Phase B, #335) |
| `src/lib/server/toolbox-drip.ts` (+ `.test.ts`), `app/api/cron/toolbox-talk-drip/route.ts` | Created (Phase C, #337) |
| `prisma/schema.prisma` (LmsNotification, LmsUser.emailOptOut) + 2 migrations | Modified/Created (verified applied to carsi-db) |
| `src/lib/server/email-templates.ts`, `transactional-email.ts` (recert + toolbox emails) | Modified |
| `.github/workflows/notifications-recert.yml`, `notifications-toolbox-drip.yml` | Created (inert until `CRON_SECRET` set) |
| `src/components/landing/HomePricingSection.tsx`, `app/page.tsx` | Created/Modified (GP-347, #339) |
| `src/lib/prisma.ts` (staleness probe), `app/api/lms/[[...path]]/route.ts` (stubs removed) | Modified |

## 5. Running state
- **No background processes running.** Remotion render outputs cleared; CARSI `.next` + `node_modules/.cache` cleared this session.
- **Prod:** carsi.com.au healthy (DO app `monkfish-app` a9d718db → `carsi-db`); notifications API live, bell real data, landing pricing live.
- The **crons are inert** — no scheduler fires them until `CRON_SECRET` is configured (see §7).

## 6. Verification — exact commands
```bash
cd /Users/phillmcgurk/CARSI
git status --short                     # clean (only untracked docs/session-handoffs/)
npm run type-check                     # GREEN
npm run test:unit                      # GREEN 201/201
gh pr list --state open                # empty (all merged)
curl -s -o /dev/null -w "%{http_code}\n" https://carsi.com.au/api/health   # 200
# DO deploy/migrate verification (needs the DO token via curl — doctl 1.163.0 is bugged):
#   curl -H "Authorization: Bearer <PAT>" https://api.digitalocean.com/v2/apps/a9d718db-7961-4107-9477-96c72fcf620f/deployments?per_page=1
```

## 7. Deferred + open questions
**Deferred (Owner / Blocking / Why):**
- **Enable the crons** — Owner: founder. Blocking: recert + drip actually firing. Why: **`CRON_SECRET` must be set on the DO app env AND as a GitHub repo secret** (same value). No scheduler existed before; the routes fail-closed (503) until then. Also un-blocks the existing `contact-reply-sla` cron.
- **Drip unsubscribe UI** — Owner: next agent. Blocking: turning on the drip *email* in prod. Why: `emailOptOut` field + suppression are wired, but there's no user-facing toggle/one-click unsubscribe (Spam Act). In-app drip is safe now.
- **GP-257/258 (E2E)** — Owner: next agent. Why: `carsi-journeys` + `pre-production` specs exist (CI E2E gate green) but cover catalogue/auth/detail/subscribe-page, NOT the full authenticated enrol→lesson→quiz→cert or a full Stripe payment flow. Left OPEN deliberately (avoided a false-complete).

**Open questions:**
- The `[NEXUS]` tickets GP-296–301 (Urgent/High) are portfolio-fleet orchestration **misfiled on the CARSI board** — re-route off CARSI (flagged in the project status update; not built).
- Add the Maintenance course slug to `DRIP_COURSE_SLUGS` to include Maintenance subscribers in the drip? (currently toolbox-course enrollees only.)
- GP-436 (High, evidence audit of ~100 Done tickets) warrants a dedicated, tooled run.

## 8. Pick up here
**Start here:** the board is accurate and there's **no clean High-priority app-build left** on CARSI. The meaningful next moves are (a) walk the founder through setting `CRON_SECRET` to activate recert/drip, (b) build the drip unsubscribe UI (unblocks drip email), (c) verify/close the GP-257/258 E2E gaps, or (d) a dedicated GP-436 audit run.
**Do NOT redo:** any merged PR (#334/#335/#337/#339); the notifications system; the landing pricing; the migration-path setup; the Linear reconciliations. Do NOT modify `eslint.config.mjs` to chase the local lint error.
**First command to run:**
```bash
cd /Users/phillmcgurk/CARSI && git fetch origin main --quiet && git log --oneline -3 origin/main && gh pr list --state open
```

## 9. Risk notes
- **`CRON_SECRET` gap** — new crons + `contact-reply-sla` are inert/fail-closed until it's set (DO env + GitHub secret).
- **DO API access from this Mac = curl only** — `doctl 1.163.0` rejects even valid PATs (bug). A valid PAT ("carsi-token") was pasted into the chat this session → **founder should delete/rotate it** (it's in the transcript). 1Password item "DigitalOcean CLI" biometric read only works in an interactive terminal, not the agent shell.
- **`npm run lint` fails locally (env anomaly), CI green** — do not chase.
- **`main = prod`** (DO deploy_on_push); founder merges prod PRs (agent self-merge classifier-blocked).
- Prod DB `carsi-db` is only reachable from inside DO; schema changes go via the PRE_DEPLOY migrate job (proven working).

## 10. Handoff quality check
type-check + unit tests actually run this session (green, cited). Merges verified via `gh pr view` + `git log origin/main` (a6bafa43). Prod verified via curl 200 + DO migrate-job logs. Shipped (merged) vs deferred clearly separated; GP-257/258 explicitly left open rather than false-completed. No process claimed running. First command provided.

Handoff complete. Next safe action: help the founder set `CRON_SECRET` (DO env + GitHub secret) to activate the recert + drip crons, or build the drip unsubscribe UI to unblock the drip email.
