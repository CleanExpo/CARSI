# Session Handoff — CARSI Courses Initiative (curation + AI-Assistant pilot + content refresh + video engine)

**Timestamp:** 2026-07-02T10-29-03 · **Branch:** `fix/courses-catalog-curation` · **Repo:** CleanExpo/CARSI
**Phase 0 gate:** GREEN — no `scripts/handoff-loop.sh` in repo; ran mandatory gate `npm run type-check` → `tsc --noEmit` clean. Git tree clean (0 uncommitted). Live site all-200.

---

## 1. Summary of what was done
Founder was embarrassed by the live CARSI courses. Full SPM-style walkthrough + multi-track execution:
- **Diagnosed (completed):** the "mess" = a raw WordPress/WooCommerce **import polluting prod** (garbled `| AUD $29 | 5 Modules` titles, junk, clean+garbled duplicate pairs) + **prod DB diverges from the repo seed files**. The LMS engine + most course *content* are fine.
- **Curation (completed, LIVE in prod):** unpublished 6 garbled-duplicate + 13 junk/off-topic courses + 3 more (stub-duplicate microbial, laser video, flow-chart) via the admin panel. Catalogue **85 → 70 published, all substantive**.
- **Flagship rescue (completed, LIVE):** "CARSI Maintenance Company Onboarding" (12-mod floor-care) was seeded-as-draft, never published → published.
- **AI Assistant pilot (completed, LIVE + shipped):** "Collaborative Development — Your Personal AI Assistant" upgraded **1 → 11 modules** (2026-current), branded **Remotion trailer** rendered + attached, **VideoObject JSON-LD + inline hero player** added, course published. **PR #320 MERGED + deployed.**
- **Content refresh (completed):** audited all 73 courses via admin API — **64 already healthy**; fleshed out the 6 genuinely-thin ones: WRT (+Monitoring module), SWMS/SOP/SDS (practical modules), insurance-adjusters (→4 modules), large-loss-mastery (→5 modules).
- **Video rollout engine (partial — blocked on hosting):** built a parameterised batch trailer renderer; **rendered all 73 course trailers** (271MB). Upload+attach pipeline ready. Blocked ONLY on Cloudinary creds.
- **Cleanup (completed):** `.curation` scratch 5.9MB → 88KB; health-checked.

## 2. Where it started
Founder: "I looked through the CARSI website … it is nothing like it should be … I am talking about the courses." Then, across the session: upgrade the AI Assistant course → add marketing video (Remotion) + SEO → publish → roll out to the rest → refresh all courses to 2026 → clear bloat + handoff. Branch cut from `main` (prod). Constraints: **prod DB unreachable from this Mac**; `main = prod` via DO deploy_on_push; prod data changes founder-gated. Full context: memory `carsi-courses-catalog-curation` and plan `~/.claude/plans/i-looked-through-the-valiant-summit.md`.

## 3. Decisions locked + what shipped
**Shipped (git, on branch `fix/courses-catalog-curation`, all in MERGED PR #320):**
- `75a9e624` — publish Maintenance course durable state in `data/seed/courses-catalog.json`
- `416842f9` — AI Assistant marketing trailer MP4 in `public/videos/ai-assistant-course-trailer.mp4`
- `0e706ab6` — VideoObject JSON-LD + inline hero video player (`public-courses-list.ts`, `courses/[slug]/page.tsx`)
- **PR #320 MERGED → main → deployed to prod** (all CI green; trailer + schema verified live).

**Applied DIRECTLY to prod (NOT in git — via founder's authed admin session at carsi.com.au/admin/courses):** all curation unpublishes, course publishes (Maintenance, AI Assistant), and all content edits (WRT/SWMS/SOP/SDS/insurance-adjusters/large-loss). These are live in the prod DB; there is no repo artifact for them (prod DB is unreachable locally). Reversible via the admin panel.

**Decisions locked:** curate-down-hard to a vetted catalogue; Remotion (not paid AI-video) for trailers; content-refresh is targeted (fix ~6 thin), not a 73-course rewrite (64 already good).

## 4. Key files
| File | Status |
|---|---|
| `data/seed/courses-catalog.json` | Modified (Maintenance course → published; committed) |
| `public/videos/ai-assistant-course-trailer.mp4` | Created (committed; served live) |
| `src/lib/server/public-courses-list.ts` | Modified (exposes `intro_video_url`; committed) |
| `app/(public)/courses/[slug]/page.tsx` | Modified (VideoObject schema + hero player; committed) |
| `.curation/upload-trailers-batch.ts` | Created — Needs review (video upload, runs when creds land; gitignored) |
| `.curation/render-trailer-batch.cjs` + `rollout-courses.json` | Created (trailer render engine + manifest; gitignored) |
| `.curation/CURATION_MANIFEST.md` + `curation.json` | Created (audit/curation record; gitignored) |
| `docs/session-handoffs/handoff-2026-07-02T10-29-03.md` | Created (this file; untracked) |

## 5. Running state
- **No background processes running.** The batch trailer render (`render-trailer-batch.cjs`) **completed: 73/73 rendered, 0 failed.**
- **73 rendered trailers (271MB)** at `/Users/phillmcgurk/Pi-Dev-Ops/remotion-studio/.remotion-renders/trailer-*.mp4` (gitignored). Kept for the pending upload; regenerable via one command if cleared.
- `CARSI/.env.local` does **not** exist (Cloudinary creds never provided).

## 6. Verification — exact commands
```bash
cd /Users/phillmcgurk/CARSI
git status --short                 # expect clean
npm run type-check                 # expect tsc --noEmit clean (PASSED on the way out)
# Live prod health (all returned 200 at handoff):
for u in /courses /courses/collaborative-development-your-personal-ai-assistant \
  /courses/insurance-adjusters-and-their-roles /courses/large-loss-mastery-course \
  /videos/ai-assistant-course-trailer.mp4; do \
  curl -s -o /dev/null -w "%{http_code} $u\n" "https://carsi.com.au$u"; done
curl -s https://carsi.com.au/courses/collaborative-development-your-personal-ai-assistant | grep -c VideoObject  # expect >=1 (live)
gh pr view 320 --json state -q .state   # expect MERGED
```

## 7. Deferred + open questions
**Deferred (work not done):**
- **Video host/attach rollout** — Owner: founder (creds) then next agent. Blocking: video-rollout completion. Why: needs Cloudinary creds in `CARSI/.env.local`; 73 trailers rendered + upload/attach scripts ready.
- **3 held curation courses** (`glass-cleaning-course`, `fundamental-business-framework`, `microbe-clean-basic-understanding-course`) — Owner: founder. Blocking: nothing. Why: have real content but off-topic; founder to keep/park.

**Open questions:**
- **large-loss-mastery-course shows 8 IICRC CECs** but content supports ~1–2h — Owner: founder (accreditation decision). Blocking: IICRC credibility. Why: CEC value not changed by agent; correct it or develop to genuine 8-CEC depth.
- Whether to delete the 271MB local trailers (regenerable) — Owner: next agent/founder.

## 8. Pick up here
**Start here:** the only blocked track is video hosting. When `CARSI/.env.local` has the 3 `CLOUDINARY_*` lines:
1. `cd /Users/phillmcgurk/CARSI && npx tsx .curation/upload-trailers-batch.ts` (uploads all 73 → Cloudinary → writes `.curation/trailer-urls.json`; resume-safe).
2. For each course, PATCH intro video: authed `POST`/`PATCH` `/api/admin/courses/{id}` with `{introVideoUrl}` (build slug→id from `GET /api/admin/courses`; run via the founder's browser session — httpOnly cookie).
3. Then resolve the large-loss CEC and the 3 held courses with the founder.

**Do NOT redo:** the SPM diagnosis; the curation (live); Maintenance + AI Assistant publishes (live); the 6 content flesh-outs (live); PR #320 (merged). 64/73 courses are already healthy — do NOT rewrite them.

**First command to run:**
```bash
[ -f /Users/phillmcgurk/CARSI/.env.local ] && grep -c CLOUDINARY /Users/phillmcgurk/CARSI/.env.local || echo "creds still missing — video track blocked"
```

## 9. Risk notes
- **Prod DB is unreachable from this Mac**; all curation/publish/content edits were made via the founder's live admin session and exist ONLY in prod (no git artifact, no local reproduction). Verify against the LIVE admin/site, not the repo.
- The API PATCH to write course modules **replaces the full modules array** — the safe write path used this session was the admin UI editor. Do not blind-PATCH module content.
- `main = prod` (DO deploy_on_push); merging deploys. PR #320 already merged/deployed.
- Trailers (271MB) are gitignored local output; regenerate with `node /Users/phillmcgurk/CARSI/.curation/render-trailer-batch.cjs` if cleared.
- `.env.local` (Cloudinary secrets) must be created by the founder; never commit it.

## 10. Handoff quality check
- type-check actually run (passed); live 200s actually curled; PR #320 merge verified via gh. No process claimed running (render confirmed finished). Shipped (committed/merged) vs prod-only-via-admin vs deferred are separated explicitly. First command provided.

Handoff complete. Next safe action: confirm whether `CARSI/.env.local` exists with 3 Cloudinary vars; if yes, run the upload+attach pipeline, otherwise the video track stays parked and everything else is done.
