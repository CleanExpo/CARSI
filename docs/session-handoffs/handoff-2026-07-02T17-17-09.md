# Session Handoff — CARSI courses stickiness, migrations, Margot a11y, cleanup

**Timestamp:** 2026-07-02T17-17-09 · **Branch:** `main` (HEAD `04798510`) · **Repo:** CleanExpo/CARSI

**Phase 0 gate:** No `scripts/handoff-loop.sh` in repo → definition-of-done gates run manually.
- `npm run type-check` (tsc --noEmit) → **GREEN**
- `npm run test:unit` (vitest) → **GREEN, 182/182 across 33 files**
- `main` CI on `04798510` (CI + Security Scanning + Lighthouse) → **all success** (`gh run list --branch main`)
- Production `carsi.com.au/api/health` → **HTTP 200**, `x-do-app-origin` (DigitalOcean-served)
- `npm run lint` locally → **exit 2 config error** (`could not find plugin "import"`) — a **local-environment anomaly, NOT a repo defect**: CI runs the identical `eslint .` and lint is green on this commit. Do NOT "fix" it by registering plugins (cascades to react-hooks and risks a redefine error that breaks CI's green lint). See §9.
- Full `next build` not run locally (CI validates it; green). **Verdict: READY** (shipping-authoritative gates green).

## 1. Summary of what was done
Very large multi-track session, almost all shipped/live:
- **Video rollout finished:** all 73 course trailers rendered (Remotion) → uploaded to Cloudinary → attached to their courses via the admin API (guarded full-echo PATCH; 0 content/price/publish loss). Verified live.
- **Air-Mover course:** created (10 modules), then **re-grounded to Australia** (230 V/10 A GPO, metric, AU-available 240 V products, AU English) after founder correction; **free, published**; **voiced Remotion trailer** (ElevenLabs via Synthex creds) attached.
- **Australian-production standard:** new skill `.claude/skills/carsi-course-production/SKILL.md` + prominent `CLAUDE.md` rule making all CARSI courses Australian-produced (PR #322).
- **Maintenance flagship stickiness:** rebuilt all 12 modules to full program depth (scenario + knowledge-check + supervisor sign-off/evidence per module; Module 12 capstone → certification); **11 graded quizzes wired** in via new admin quiz-lesson support; **Toolbox Talks companion course** expanded to **20 WHS/site-hazard talks**.
- **Team training records:** supervisor view `/dashboard/team/records` + CSV export (#325).
- **Course-card placeholders:** consistent discipline-colour system + cleared 10 stock images (#321 + admin data pass).
- **Platform/infra:** notifications-system scope doc (#326); production migration runner reworked from Vercel to a **DigitalOcean PRE_DEPLOY job** (#330).
- **Fixed Margot PR #329** (a11y colour-contrast on the chat bubble) — verified green, merged.
- **Cache/bloat cleared** (~536 MB) + this health check.

## 2. Where it started
Continuation of the CARSI "courses are embarrassing" initiative. Across the session the founder directed: finish the video rollout; build/refresh the Air-Mover course then Australianise it; make "Australian-produced" a must-have skill; make the flagship Maintenance subscription a *must-have* (stickiness); wire graded quizzes; build team records; scope + unblock notifications; fix PR #329; check PR/merge/build health; clear bloat + health-check. Constraints: **prod DB is Supabase, reached via the DigitalOcean-served app**; `main = prod` deploy-on-push; prod data changes founder-gated; agent cannot self-merge prod-deploy PRs (classifier-blocked). Full context: memory `carsi-courses-catalog-curation`, `carsi-courses-australian-produced`.

## 3. Decisions locked + what shipped
**Shipped to `main` (all merged this session):** #321 placeholder consistency · #322 Australian-produced standard · #323 stickiness spec doc · #324 admin quiz-lesson support · #325 team training records · #326 notifications scope doc · #327 (Vercel migration runner, superseded) · #330 DigitalOcean PRE_DEPLOY migration job · #329 Margot bubble (a11y-fixed). #328 + #331 **closed**. HEAD `04798510`.

**Live in prod via the admin API (NOT in git — Supabase DB, unreachable locally):** all course content (12-module Maintenance rebuild, 11 quizzes, air-mover course + AU rewrite + voiced trailer, 20 toolbox talks, 73 trailer attachments, discipline assignments + image clears, free/publish/CEC settings). Reversible via the admin panel; verify against the LIVE site, not the repo.

**Decisions:** all CARSI courses Australian-produced; low-profile-not-primary thesis for air movers; graded quizzes via admin authoring (not seed); migrations via DO PRE_DEPLOY (not Vercel); Maintenance = verified-competency + records system.

## 4. Key files
| File | Status |
|---|---|
| `.claude/skills/carsi-course-production/SKILL.md`, `CLAUDE.md` | Created/Modified (AU standard; #322) |
| `src/lib/admin/admin-courses-service.ts` (+ `.test.ts`), `src/lib/server/course-catalog-sync.ts` | Modified (quiz-lesson support; #324) |
| `src/lib/server/team-records.ts` (+ `.test.ts`), `app/api/lms/teams/records/route.ts`, `app/(dashboard)/dashboard/team/records/page.tsx`, `app/(dashboard)/dashboard/team/page.tsx` | Created/Modified (#325) |
| `src/components/lms/CourseTextThumbnail.tsx` | Modified (neutral accent; #321) |
| `Dockerfile.migrate`, `scripts/do-migrate.sh`, `app.yaml` | Created/Modified (DO PRE_DEPLOY migrate job; #330) |
| `docs/onboarding/floor-care/06-stickiness-and-learning-design.md`, `docs/specs/2026-07-02-notifications-drip-recert-scope.md` | Created (#323, #326) |
| `data/seed/floor-care-quizzes.json` | Read-only (source for the 11 quizzes wired via admin) |
| `.curation/*` (air-mover drafts, trailer-urls.json, upload-one.ts, render/upload scripts) | Local scratch (gitignored; kept) |
| `docs/session-handoffs/handoff-2026-07-02T17-17-09.md` | Created (this file; untracked) |

## 5. Running state
- **No background processes running.** All render/upload batches completed earlier; the trailer render outputs were **deleted during cleanup** (regenerable via `.curation/render-trailer-batch.cjs` + `remotion-studio`).
- Cleared: `remotion-studio/.remotion-renders` (283 MB), `remotion-studio/.ffbin`, CARSI `.next` (253 MB) — ~536 MB freed.
- `node_modules` reinstalled via `npm ci` this session.

## 6. Verification — exact commands
```bash
cd /Users/phillmcgurk/CARSI
git status --short                     # clean (only untracked docs/session-handoffs/)
npm run type-check                     # GREEN
npm run test:unit                      # GREEN 182/182
gh run list --branch main --limit 3    # CI/Security/Lighthouse: success on 04798510
curl -s -o /dev/null -w "%{http_code}\n" https://carsi.com.au/api/health   # 200
curl -sI https://carsi.com.au/ | grep x-do-app-origin                      # confirms DO-served
# NOTE: `npm run lint` errors locally (env anomaly) — CI's lint is green; do not chase it.
```

## 7. Deferred + open questions
**Deferred (work not done):**
- **DO migration dry-run** — Owner: founder / DO access. Blocking: notifications Phase A (any schema change). Why: #330 defines the PRE_DEPLOY `migrate` job in `app.yaml`, but **DigitalOcean does not auto-apply `app.yaml` spec changes on push** — the app spec must be applied (DO dashboard → App Spec, or `doctl apps update --spec app.yaml`), and the job needs `DATABASE_URL` + `DATABASE_CA_CERT`. `doctl` is not installed here and there is no DO API token, so the dry-run + its log couldn't be run/watched from this machine.
- **Notifications Phase A** (LmsNotification model + service + real endpoints + live bell) — Owner: next agent, after the migration path is proven. Blocking: the DO migrate dry-run above (a new model needs a working migration path).
- **RIA report → wiki + Drive** — Owner: founder pointers. Blocking: (a) which report (the only RIA PDF in `phill.mcgurk@gmail.com` is the rejected NRPG "State of the Industry"; the intended one is likely in a CARSI mailbox reachable only via the founder's Chrome session — the Gmail connector can't download attachments), (b) the Hermes wiki isn't on this Mac (no `WIKI_PATH`); fallback = a Google Doc in a new "RIA Reports" Drive folder. Founder chose: fetch via Chrome session, folder "RIA Reports", wiki = Hermes.
- **Maintenance stickiness platform features** beyond records — Owner: next agent. Monthly Toolbox-Talk drip + recert reminders need the notifications system (Phase A→C in the scope doc).

**Open questions:**
- Air-Mover course CECs — left unset (founder decision; it's a "CARSI knowledge course, no CEC").
- Whether to add `eslint-plugin-import` as an explicit devDep to make `eslint .` robust locally (would need testing it doesn't break CI's plugin resolution).

## 8. Pick up here
**Start here:** the one true blocker is the **DigitalOcean migration path**. Apply `app.yaml` to the DO `carsi` app (dashboard App Spec or `doctl apps update <app-id> --spec app.yaml`), confirm the `migrate` job has `DATABASE_URL` + `DATABASE_CA_CERT`, trigger a deploy, and read the `migrate` job log — with no pending migrations it should log "No pending migrations" and pass (the dry-run). Once green, build **notifications Phase A**.
**Do NOT redo:** any merged PR (#320–#330); the live course content (all in prod via admin — 12-module Maintenance rebuild, 11 quizzes, air-mover AU rewrite + voiced trailer, 20 toolbox talks, 73 trailers, placeholder/discipline data pass); the Margot #329 a11y fix. Do NOT modify `eslint.config.mjs` to chase the local lint error.
**First command to run:**
```bash
cd /Users/phillmcgurk/CARSI && git status --short && gh run list --branch main --limit 3
```

## 9. Risk notes
- **Prod DB is Supabase, reached via the DO-served app; unreachable from this Mac.** All course/quiz/records changes exist only in prod (no git artifact). Verify against the live admin/site.
- **`npm run lint` fails locally (exit 2, plugin-resolution) but CI's identical `eslint .` is green.** Treat as an environment anomaly; the config is correct in CI. Registering plugins locally cascades (import→react-hooks→…) and risks a redefine error that breaks CI.
- **#330's DO migrate job is inert until the app spec is applied on DO** (git push does not apply `app.yaml` job changes). If the DO app's `DATABASE_URL` turns out to be a pooler (not a direct connection), the PRE_DEPLOY job will fail P1000 and **abort deploys** — recoverable (revert #330 / fix the URL), and it applies nothing (no pending migrations).
- `main = prod` (DigitalOcean deploy-on-push); agent cannot self-merge prod-deploy PRs (classifier-blocked) — founder merges.
- ElevenLabs creds live in `Synthex/.env.local` (not `.env`); voiced Remotion renders need the bundled ffprobe via a PATH/DYLD wrapper (recorded in memory).

## 10. Handoff quality check
type-check + unit tests actually run this session (green, cited). CI/main + prod verified via `gh`/`curl`. Lint failure reported honestly with its exit code + why it's environmental (not masked). Shipped (merged PRs) vs live-in-prod-via-admin vs deferred are separated. No process claimed running (renders confirmed deleted). First command provided.

Handoff complete. Next safe action: apply the `app.yaml` spec to the DigitalOcean `carsi` app and run the migrate job's dry-run (or hand that DO step to the founder), which unblocks notifications Phase A.
